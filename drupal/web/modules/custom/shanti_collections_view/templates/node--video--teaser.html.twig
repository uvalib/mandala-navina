{#
/**
 * @file
 * Video node, 'teaser' view mode -- the card shown in a collection's
 * Audio & Video gallery. Renders the shared shanti-thumbnail component (see
 * that file's docblock); variables come from
 * shanti_collections_view_preprocess_node(). `video` has no
 * field_thumbnail_image (only `audio` does -- see AV9's scoping note), so
 * `thumbnail` is always empty here and the Kaltura-derived
 * default_image_url is the only image source.
 */
#}
{% include '@shanti_collections_view/shanti-thumbnail.html.twig' with {
  url: url,
  thumbnail: null,
  default_image_url: thumbnail_default_image_url,
  icon: 'video',
  title: label,
  badge: thumbnail_badge,
  meta: thumbnail_meta,
  footer_title: null,
  footer_url: null,
  extra_class: 'video',
} only %}
