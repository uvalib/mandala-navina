<VirtualHost *:80>
	# The ServerName directive sets the request scheme, hostname and port that
	# the server uses to identify itself. This is used when creating
	# redirection URLs. In the context of virtual hosts, the ServerName
	# specifies what hostname must appear in the request's Host: header to
	# match this virtual host. For the default virtual host (this file) this
	# value is not decisive as it is used as a last resort host regardless.
	# However, you must set it for any further virtual host explicitly.
	#ServerName www.example.com

	ServerAdmin webmaster@localhost

	# Rewritten to ${APACHE_DOCUMENT_ROOT} by the sed pass in package/Dockerfile,
	# which is why this file is installed to sites-available (the sed globs
	# sites-available/*.conf) and must keep the literal /var/www/html here.
	DocumentRoot /var/www/html

	# Available loglevels: trace8, ..., trace1, debug, info, notice, warn,
	# error, crit, alert, emerg.
	# It is also possible to configure the loglevel for particular
	# modules, e.g.
	#LogLevel info ssl:warn

	ErrorLog ${APACHE_LOG_DIR}/error.log
	CustomLog ${APACHE_LOG_DIR}/access.log combined

	# For most configuration files from conf-available/, which are
	# enabled or disabled at a global level, it is possible to
	# include a line for only one particular virtual host. For example the
	# following line enables the CGI configuration for this host only
	# after it has been globally disabled with "a2disconf".
	#Include conf-available/serve-cgi-bin.conf

	ProxyPreserveHost On
	ProxyRequests Off
	# Mount SimpleSAMLphp under the same host as Drupal: the SP web endpoints are
	# served by the netbadge-0 container (network alias "sp"), while the
	# composer-vendored SP library validates the session in-process via
	# SIMPLESAMLPHP_CONFIG_DIR. Both halves share config + the Redis session store.
	ProxyPass        "/simplesaml/"  "http://sp:80/simplesaml/"
	ProxyPassReverse "/simplesaml/"  "http://sp:80/simplesaml/"

	SetEnv SIMPLESAMLPHP_CONFIG_DIR /var/simplesamlphp/config

</VirtualHost>
