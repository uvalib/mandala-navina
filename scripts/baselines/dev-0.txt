# Baseline: dev-0 (mandala-drupal-dev-0)
#
# D7 source: mandala_d7_images on rds-mysql8-staging, loaded 2026-07-17 from
#            production (287,939 node rows). NOT the same dump as local DDEV,
#            which runs the 2026-07-07 staging dump (288,023 node rows) — the
#            two environments differ on several keys and neither is wrong.
#            See docs/deferred/canonical-d7-dev-source-dump.md.
#
# Usage:  EXPECT_FILE=scripts/baselines/dev-0.txt ./scripts/migration-cycle.sh validate
#
# PROVENANCE: dev-0 was rebuilt FROM SCRATCH on 2026-08-26/27 (see
# docs/session-logs/2026-08-26-dev0-rebuilt-from-scratch.md and
# docs/session-logs/2026-08-27-migration-complete-and-verified.md), not rolled
# back and re-imported. All values below are read from that clean run:
#   - node:shanti_image and entity:path_alias/group_path_alias were SOURCE-DERIVED
#     first (2026-08-25, against mandala_d7_images directly) and the completed
#     run matches them exactly — 111,340/111,340, 0 mismatches.
#   - integrity:legacy_nid_dupes = 0. The duplicate that made the PREVIOUS
#     rollback→import baseline circular (D7 nid 981206 -> two D11 nodes) did
#     NOT recur on this from-scratch build — confirms it was an artefact of a
#     resumed OOM'd migration, not a migration defect.
#   - The paragraph/field counts (previously "not yet derived" — they depend on
#     the source plugins' per-reference join logic, not a simple D7 table count)
#     are now taken from this verified-clean run: 0 errors in the log, exact
#     node-count match, and the uniqueness check above, so there is no
#     circularity concern in trusting them.

node:shanti_image 111340
paragraph:image_agent 111345
paragraph:image_descriptions 55041
paragraph:external_classification 9
term:external_classification_scheme 2
field:field_subjects 79337
field:field_places 68755
field:field_kmap_terms 61668
field:field_kmap_collections 83494
entity:path_alias 111301
entity:group_path_alias 171
integrity:legacy_nid_dupes 0
